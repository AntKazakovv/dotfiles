import {IContactsConfig} from 'wlc-engine/modules/core/system/interfaces/base-config/contacts.interface';

export const contactsConfig: IContactsConfig = {
    estResTime: 24,
    timeUnits: gettext('hours'),
};
