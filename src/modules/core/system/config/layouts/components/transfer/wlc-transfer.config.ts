import {ILayoutComponent} from 'wlc-engine/modules/core';

export namespace wlcTransfer {
    export const def: ILayoutComponent = {
        name: 'transfer.wlc-transfer',
        params: {
            wlcElement: 'block_profile-transfer',
        },
    };
}
