import {ILayoutComponent} from 'wlc-engine/modules/core';

export namespace wlcCounter {
    export const bonuses: ILayoutComponent = {
        name: 'core.wlc-counter',
        params: {
            counter: 'bonuses-main',
        },
    };
}
