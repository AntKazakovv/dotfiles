import {ILogTypes} from './index';

export const errorTypes: ILogTypes = {
    '24.0.0': {
        // Error when trying to subscribe or unsubscribe from mailing list.
        level: 'error',
    },
};
